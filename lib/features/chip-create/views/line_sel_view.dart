import 'package:flutter/material.dart';

class LineSelView extends StatelessWidget {
  const LineSelView({super.key});

  @override
  Widget build(BuildContext context) {
    return Row(mainAxisAlignment: MainAxisAlignment.spaceAround, children: [Text('data'), Text('abc')]);
  }
}
