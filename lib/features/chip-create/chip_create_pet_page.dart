import 'package:flutter/material.dart';

class ChipCreatePetPage extends StatelessWidget {
  const ChipCreatePetPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Create Pet')),
      body: Text('--'),
    );
  }
}
